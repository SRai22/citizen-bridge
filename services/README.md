# Services

Each deployable service gets its own directory here when its implementation
ticket starts. Copy `service-template/`, rename the package metadata, and add
only the adapters that service needs.

The legacy application remains in `/backend` during the strangler transition.
