name: Backend CI

on:
  push:
    paths:
      - "backend/**"
      - "docker-compose.yml"
      - "docker-compose.dev.yml"
      - ".github/workflows/backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - "docker-compose.yml"
      - "docker-compose.dev.yml"
      - ".github/workflows/backend.yml"

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      DATABASE_URL: sqlite:///./test.db
      JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-only-jwt-secret-at-least-32-characters' }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD || 'ci-only-postgres-password' }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      MOCK_AI: "true"
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install dependencies
        run: python -m pip install -e ".[dev]"

      - name: Lint
        run: ruff check .

      - name: Check formatting
        run: ruff format --check .

      - name: Type-check
        run: mypy app/

      - name: Test
        run: pytest --cov=app/core --cov-report=term-missing --cov-fail-under=80
