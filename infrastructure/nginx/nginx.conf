events {}

http {
    server {
        listen 80;

        location = /health {
            return 200 'gateway foundation ready\n';
            add_header Content-Type text/plain;
        }

        # Service upstreams and strangler routes are added by BE-002.
    }
}
