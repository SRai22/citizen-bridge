import { SectionPage } from "@/components/section-page";

export default function ActivityPage() {
  return <SectionPage eyebrow="What changed" title="Recent Activity" description="Important updates across cases, documents, and delegated access will appear here." />;
}
